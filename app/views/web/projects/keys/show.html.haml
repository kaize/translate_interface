.page-header
  - if can? :project_keys
    = link_to project_keys_path, :class => 'btn' do
      %i.icon-list-alt
      = t('simple_form.back')
  %h1= t('.listing')

%dl
  %dt= t('.name')
  %dd= @key.name
