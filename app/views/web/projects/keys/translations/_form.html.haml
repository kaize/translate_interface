
= simple_form_for([@translation.project, @translation.key, @translation], :defaults => { :input_html => { :class => 'input-xlarge' } }) do |f|
  = f.error_notification

  .form-inputs
    = f.input             :data

  .form-actions
    = f.button :submit, :class => "btn-primary"
