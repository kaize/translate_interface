
= simple_form_for([@project_locale.project, @project_locale], :defaults => { :input_html => { :class => 'input-xlarge' } }) do |f|
  = f.error_notification

  .form-inputs
    = f.input :locale, :collection => Locale.web

  .form-actions
    = f.button :submit, :class => "btn-primary"
